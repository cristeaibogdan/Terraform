# Terraform notes

To start using terraform tf files:

1. Install terraform on your machine
2. Open a terminal cmd line in one of the respective folders
3. Run terraform init
4. Run terraform plan
5. Run terraform apply
6. [Optional] Run terraform destroy to remove all rss in the current directory
